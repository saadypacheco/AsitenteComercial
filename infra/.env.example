# infra/.env  —  copiar a .env (NO commitear). Lo lee docker-compose.yml
WAHA_API_KEY=                 # clave fuerte para proteger la API del bridge
WEBHOOK_SECRET=               # HMAC compartido con backend/.env (mismo valor)

# Generar claves fuertes:
#   PowerShell: [Convert]::ToBase64String((1..32 | %{ Get-Random -Max 256 }))
